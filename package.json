{
  "name": "landing-zillow-like",
  "version": "1.0.0",
  "description": "A simple landing page with lead capture form styled with a Zillow-like palette (Node + TypeScript)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && node copy-static.js",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
